{
	"name": "@ovp-proto/types",
	"version": "0.1.0",
	"license": "LGPL-3.0-only",
	"exports": {
		"./*": "./dist/generated/*.js",
		"./utils/*": "./dist/src/utils/*.js"
	},
	"devDependencies": {
		"ncp": "^2.0.0",
		"rimraf": "^6.0.1",
		"ts-proto": "^2.7.7",
		"typescript": "^5.9.2"
	},
	"scripts": {
		"copy-files": "ncp schema dist/schema",
		"clean": "rimraf dist/ && rimraf generated/",
		"proto:generate": "node scripts/generate-types.js",
		"build": "yarn clean && yarn proto:generate && tsc && yarn copy-files"
	}
}
