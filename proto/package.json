{
	"name": "@ovp-proto/types",
	"version": "0.1.0",
	"license": "LGPL-3.0-only",
	"exports": {
		"./*": "./dist/generated/*.js"
	},
	"devDependencies": {
		"rimraf": "^6.0.1",
		"ts-proto": "^2.7.7",
		"typescript": "^5.9.2"
	},
	"scripts": {
		"clean": "rimraf dist/",
		"proto:generate": "node scripts/generate-types.js",
		"build": "yarn clean && tsc"
	}
}
