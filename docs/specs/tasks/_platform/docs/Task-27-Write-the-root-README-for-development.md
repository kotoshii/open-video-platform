## Write the root README for development

Needs: [Task-26 — Write the root README for preview and production](Task-26-Write-the-root-README-for-preview-and-production.md),
[infrastructure Task-14 — Start every app with one command](../infrastructure/Task-14-Start-every-app-with-one-command.md)

Add the development setup to the root README: the infrastructure from `compose.dev.yaml`, the apps on the host with
`yarn dev`, what to install locally (Node from `.nvmrc`, FFmpeg for the processing worker), and `yarn seed` for test
data.
