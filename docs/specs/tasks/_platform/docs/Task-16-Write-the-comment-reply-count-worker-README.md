## Write the comment-reply-count-worker README

Write `apps/workers/comment-reply-count-worker/README.md`: what it does, the topic it consumes and the database it
writes (comment-api's), its environment variables, and how to run it.
