## Write the search-api README

Write `apps/api/search-api/README.md`: a short overview of what the service owns, taken from
[service-map.md](../../../service-map.md), its environment variables, and how to run it — including the Elasticsearch
image with the Ukrainian plugin it needs. Check its Swagger docs while there: every endpoint shows its request, its
responses and its error codes.
