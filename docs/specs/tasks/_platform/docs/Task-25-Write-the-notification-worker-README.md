## Write the notification-worker README

Write `apps/workers/notification-worker/README.md`: what it does, the topics it consumes and the database it writes
(notification-api's), its jobs, its environment variables, and how to run it.
