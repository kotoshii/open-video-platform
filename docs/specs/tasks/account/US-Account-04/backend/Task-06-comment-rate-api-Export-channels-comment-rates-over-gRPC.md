## comment-rate-api: Export channels' comment rates over gRPC

Add the gRPC method that takes a list of channel ids and returns the rates they gave comments: the comment, like or
dislike, and when it was set.
