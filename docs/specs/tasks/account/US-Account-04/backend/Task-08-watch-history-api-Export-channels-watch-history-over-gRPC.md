## watch-history-api: Export channels' watch history over gRPC

Add the gRPC method that takes a list of channel ids and returns their watch history — the video, its stored title
and when it was last watched — and whether each history is paused.
